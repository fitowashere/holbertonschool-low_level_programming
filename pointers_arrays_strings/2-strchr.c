#include "main.h"

/**
 **_strchr- gets users input
 *@s: stores first input
 *@c: stores second input
 *Return: result
 */

char *_strchr(char *s, char c)
{

	int i = 0;
	while (s[i] != NULL)
	{
		if (s[i] == c)
			return [c];
		else
			return NULL;


	}
}
